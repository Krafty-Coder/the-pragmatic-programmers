# Which of the following will match?

# 1 – a = [1, 2, 3]               # will match
# 2 – a=4                         # will match
# 3 – 4=a                         # will match
# 4 – [a, b] = [ 1, 2, 3 ]        # will not match
# 5 – a = [ [ 1, 2, 3 ] ]         # will match
# 6 – [a] = [ [ 1, 2, 3 ] ]       # will match
# 7 – [[a]] = [ [ 1, 2, 3 ] ]     # will not match

# => [1, 2, 3, 5, 6]


